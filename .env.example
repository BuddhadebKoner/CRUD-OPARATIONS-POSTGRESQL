# Database Configuration
DB_HOST=localhost
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_NAME=express-crud
DB_PORT=5432

# Server Configuration
PORT=3000
NODE_ENV=development

# CORS: Allowed origin(s). Use * for development, specific domain for production.
CORS_ORIGIN=*
